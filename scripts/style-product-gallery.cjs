const fs=require('fs');for(const name of ['embera','aether','flavo','fera','ignite']){const f=name+'.html';let s=fs.readFileSync(f,'utf8');s=s.replace('</head>','<link rel="stylesheet" href="assets/css/product-gallery-editorial.css?v=1"></head>');const marker='<aside class="epp-buy-panel"';const pos=s.indexOf(marker);const before=s.slice(0,pos);const end=before.lastIndexOf('</div>');s=before.slice(0,end)+'<p class="epp-gallery-hint"><span class="lang-ro">Selectează un cadru pentru detalii.</span><span class="lang-en" hidden>Select a view to explore the details.</span><span aria-hidden="true">↗</span></p>\n'+before.slice(end)+s.slice(pos);fs.writeFileSync(f,s);}
